import HomePage from "@/components/home/HomePage";

/**
 * Home: carousel hero, safety banner, pillars, program highlights, how it works,
 * impact metrics, testimonials, matching flow, and closing CTA.
 *
 * Section ids for anchors: #pillars, #how-it-works, #promises, #testimonials,
 * #matching.
 */
export default function Home() {
  return <HomePage />;
}
